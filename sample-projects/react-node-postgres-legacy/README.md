# React Node Postgres Legacy

Legacy React sample with:
- React 16.13
- older create-react-app stack
- Express backend hint
- PostgreSQL connection string
- axios, cors, lodash, moment, prop-types, and classnames in a legacy dependency mix
- inline secrets, PII hints, and branching-heavy UI logic
- redundant customer summary paths and duplicated approval rules
- Node 12 and npm 6 engine hints plus intentionally older package versions so the analyzer has obvious support gaps to flag

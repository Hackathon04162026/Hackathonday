# Worker 4 Docker and CI Fixtures

These fixtures model a normalized workspace with Docker and CI/runtime configuration signals.

- `sample-workspace/Dockerfile`: direct and indirect Docker base image examples.
- `sample-workspace/.github/workflows/ci.yml`: GitHub Actions runtime setup examples.
- `expected-detectors.json`: expected detector findings for the sample workspace.
